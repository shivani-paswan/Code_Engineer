import 'package:flutter/material.dart';

class PlaylistSettings extends StatefulWidget {
  const PlaylistSettings({Key? key}) : super(key: key);

  @override
  State<PlaylistSettings> createState() => _PlaylistSettingsState();
}

class _PlaylistSettingsState extends State<PlaylistSettings> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
